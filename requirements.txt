huggingface_hub==1.12.0
# macOS (Apple Silicon) backend
mlx==0.31.2; platform_system == "Darwin"
mlx-metal==0.31.2; platform_system == "Darwin"
mlx-whisper==0.4.3; platform_system == "Darwin"

# Windows/Linux backend
faster-whisper>=1.1.0; platform_system != "Darwin"
# Fallback when CTranslate2 / faster-whisper cannot load (e.g. missing VC++ runtime on Windows)
openai-whisper>=20231117; platform_system != "Darwin"
numba==0.65.1
numpy==2.4.4
scipy==1.17.1
soundfile>=0.12.1
tiktoken==0.12.0
tqdm==4.67.3
gradio==6.13.0
gradio_client==2.5.0
hf-gradio==0.4.1
# speaker diarization (optional — only needed if "تمييز المتحدثين" is enabled)
pyannote.audio>=3.1.0
torch>=2.1.0
